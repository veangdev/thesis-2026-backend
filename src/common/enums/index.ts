/**
 * Single source of truth for enums: re-exported from the generated Prisma
 * client so the application and database always agree. The generated enums are
 * `const` objects plus matching union types, which work with `@IsEnum()` and as
 * TypeScript types.
 */
export {
  Role,
  AssessmentPeriodStatus,
  AssessmentStatus,
  CoachingScope,
  CoachingStatus,
  NotificationType,
} from '../../../generated/prisma/enums';
